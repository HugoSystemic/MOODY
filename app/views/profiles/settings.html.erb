<h1>Paramètres du compte</h1>
<hr>

<div class="card p-4 shadow-sm mb-4">
  <h4 class="card-title">Options de session</h4>

  <%# La déconnexion est déjà gérée par Devise et nécessite une requête DELETE %>
  <%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, data: { turbo: true }, class: "btn btn-danger" %>
</div>


<div class="card p-4 shadow-sm border-danger">
  <h4 class="card-title text-danger">Zone de danger</h4>

  <p>Cette action est irréversible. Toutes vos données (chats, musiques) seront supprimées.</p>

  <%# La suppression est gérée par Devise et nécessite une requête DELETE sur la route registration %>
  <%= button_to "Supprimer mon compte définitivement",
                registration_path(current_user),
                method: :delete,
                data: { confirm: "Êtes-vous ABSOLUMENT sûr de vouloir supprimer votre compte et toutes vos données ?", turbo: true },
                class: "btn btn-danger" %>
</div>

<%= link_to "Retour au profil", profile_path, class: "btn btn-outline-secondary mt-3" %>
