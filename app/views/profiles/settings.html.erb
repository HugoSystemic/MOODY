<div class="container mt-5" style="max-width: 700px;">
  <h1 class="mb-4 text-center">Paramètres du compte</h1>

  <div class="card shadow-sm p-4 mb-4">
    <div class="d-flex justify-content-center">
      <%= button_to "Se déconnecter",
                    destroy_user_session_path,
                    data: { turbo_method: :delete, confirm: "Voulez-vous vraiment vous déconnecter ?" },
                    class: "btn btn-danger" %>
    </div>
  </div>

  <div class="card shadow-sm p-4 border-danger mb-4">
    <h4 class="card-title text-danger mb-3">Zone de danger</h4>
    <p>Cette action est irréversible. Toutes vos données (chats, musiques) seront supprimées.</p>
    <div class="d-flex justify-content-center">
      <%= button_to "Supprimer mon compte définitivement",
                    registration_path(current_user),
                    method: :delete,
                    data: { confirm: "Êtes-vous ABSOLUMENT sûr de vouloir supprimer votre compte et toutes vos données ?", turbo: true },
                    class: "btn btn-danger" %>
    </div>
  </div>

  <div class="text-center mt-3">
    <%= link_to "Retour au profil", profile_path, class: "btn btn-outline-secondary" %>
  </div>
</div>
